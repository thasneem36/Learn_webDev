let todo = []

todo.push('learn js');
todo.push('photshop');
todo.push('path');
todo.push('python');

// todo.forEach(function(todo,pos){
//     console.log(`${pos+1} ${todo}`);
// })

// console.log("----------------------------")

for(i=0; i<todo.length; i++)
    {
        console.log(`${i+1} ${todo[i]}`);
    }